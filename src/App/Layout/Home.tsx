import bg from './TD lobby.png'

const bgStyle = {
    width: '100%'
}


export default function HomePage() {
    return (
        // <img src={bg} style={bgStyle} />
        <a>work on image sizing, consider getting started with media queries</a>
    )
}